<div class="container px-5 px-sm-0">
  <div class="row">
  
    <div class="col-md-3">
    
     <h1>User info</h1>
    
      <%= image_tag @user.get_profile_image(100,100) %>
      <% if @user.id == current_user.id %>
      <% end %>
    
      <table class="table">
        <tbody>
          <tr></tr>
          <tr>
            <th>name</th>
            <th><%= @user.name %></th>
          </tr>
          <tr>
            <th>introduction</th>
            <th><%= @user.introduction %></th>
          </tr>
        </tbody>
      </table>
      
      <div class="row">
        <% link_to edit_book_path(@user) %>
          <a class="btn btn-outline-secondary btn-block" >
            <i class="fas fa-user-cog"></i>
          </a>
      </div>
      
      <h1>New book</h1>
      
      <%= form_with model: @book do |f| %>
        <h4>Title</h4>
        <%= f.text_field :title %>
      
        <h4>Opinipn</h4>
        <%= f.text_area :body %>
      
        <%= f.submit 'Create Book' %>
      
      <% end %>
    </div>
      
      
      
      
      <div class="col-md-8 offset-md-1">
      <h1>Users</h1>
      
      <table class="table table-hover table-inverse">
      <thead>
        <tr>
        <th>image</th>
        <th>name</th>
        <th></th>
        </tr>
      </thead>
      
      <% @books.each do |book| %>
      <thead>
        <tr>
        <th><%= image_tag @user.get_profile_image(80,80) %></th>   
        
        <th><%= book.title %></th>
      
        <th><%= link_to "show", user_path(book.id) %></th>
      
        </tr>
      </thead>
      
      
      <% end %>
      
      </table>
    </div>
  </div>
</div>